Test the Soundtrack Your Brand API connection using MCP tools:

1. Use `search_account` to find the user's Soundtrack account: $ARGUMENTS
2. Use `list_locations` to show all locations
3. Use `list_sound_zones` to show all zones and their player status
4. For each active zone, use `get_now_playing` to show current track
5. Report a summary of all discovered accounts, locations, and zones
